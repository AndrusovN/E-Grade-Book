name: E Grades Book Project CI
run-name: ${{ github.actor }} is running CI for E Grades Book
on: [push]
jobs:
  TestBackend:
    runs-on: python:alpine
    steps:
      - run: pip install -r requirements.txt
      - run: pytest
  TestFrontend:
    runs-on: node:lts-iron
    steps:
      - run: npm install yarn
      - run: yarn install
      - run: yarn test
      
